# Paketler yüklenir
pkgs.to.install <- c(
  "dplyr", "magrittr", "ggplot2", "factoextra", "gridExtra", "ClusterR", "fpc", "cluster", "stringr", 
  "GGally", "NbClust", "mclust", "clValid", "tidyverse", "dendextend")

install.packages(pkgs.to.install)
